# Copy this file to .env and fill in your real key to enable live LLM mode.
# Without this, the app runs fully in mock mode (deterministic fallback,
# no API calls, no cost) -- useful for development and CI.
ANTHROPIC_API_KEY=sk-ant-your-key-here
